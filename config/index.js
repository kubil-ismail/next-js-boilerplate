import env from "./env.config";
import url from "./url.config";

/*
    -------------------------------
    EXPORT ALL CONFIG
    -------------------------------
*/

const config = {
  env,
  url,
};

export default config;
